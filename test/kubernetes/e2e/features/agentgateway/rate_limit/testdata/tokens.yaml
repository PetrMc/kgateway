apiVersion: policy.kgateway.dev/v1alpha1
kind: TrafficPolicy
metadata:
  name: token-rate-limit
spec:
  targetRefs:
  - name: my-gateway
    kind: Gateway
  rateLimit:
    local:
      tokenBucket:
        maxTokens: 50
        tokensPerFill: 10
        fillInterval: 10s
        type: tokens
